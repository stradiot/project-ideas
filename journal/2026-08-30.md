- [[home-assistant-rotary-controller]] — read the HA WebSocket API off the wire: `get_states` is 134 KB for 284 entities with no filter argument, `subscribe_events` filters by event type only so the entity filter turns out to be `subscribe_trigger`, and a group entity re-emits its whole aggregate once per member report — 15 events to a member's 3 for one dim.
