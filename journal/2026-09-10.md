- [[subghz-collar-remote-clone]] — chose RMT continuous TX mode for the burst (88 entries plus an end marker, one loop batch covers ~24 s) and showed the 88-run frame loops with a real edge at every seam, because a transmission ending on a LOW run always loses it and the RMT replays level bits verbatim; whether the seam adds time is left to an SDR comparison of frame periods.
- [[subghz-collar-remote-clone]] — settled the RMT clock chain: k = 78 ticks per symbol at a 2.675 µs tick, +14.38 ppm, identical on both firmware paths, after finding that the rounding error belongs to the rate rather than to how it is divided, so sharing the peripheral with the onboard LED costs resolution and not accuracy.
