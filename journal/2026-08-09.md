- [[subghz-collar-remote-clone]] — built the GNU Radio VM and captured the remote properly: three taps plus a 4.1 s hold, tuned 250 kHz off-centre, after tracing a phantom third mark on the waterfall to third-order intermodulation from receiver overload
- [[subghz-collar-remote-clone]] — traced the 70% beep failure to a damaged capture rather than a wrong encoding, refactored the payload to run-length ticks plus one sweepable scalar, and built an IQ analysis pipeline validated against synthetic captures
