- [[home-assistant-rotary-controller]] — questionnaire closed bar the one question that needs the wire (26 of 27): no outbound queue because the disconnect gate fires before a command is formed, text entry refused on the control path because on one encoder it is the enum carousel with a bigger array, and "true, or it says it does not know" as the tiebreaker after the blind-controller fallback turned out not to exist.
