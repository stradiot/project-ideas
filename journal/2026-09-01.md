- [[subghz-collar-remote-clone]] — decoded shock level 0 to a tick-accurate bit string (111 ticks/frame, 89 runs, against the beep's 109/88), after two hours of URH configuration in which a 2× sample-rate display setting, a dBm readout with an undocumented reference offset, and a fixed-scale demodulated view each looked like a property of the signal; procedure written up as [[urh-ook-capture-analysis]].
