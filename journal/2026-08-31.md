- [[home-assistant-rotary-controller]] — measured the command latency: three numbers, not two (25 ms API round trip with nothing matched, ~111 ms to HA's `result`, ~223 ms to the reported value), and decided the send path is self-clocked on `result` with a per-domain period floor; a six-command burst killed `context.id` as a way to attribute an event to the command that caused it.
