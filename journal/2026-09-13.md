- [[subghz-collar-remote-clone]] — loop-seam question closed: the RMT's wrap costs nothing, after the first instrument was defeated by `esp_cpu_get_cycle_count()` being a performance counter that runs 2370 ppm slow
- [[subghz-collar-remote-clone]] — RMT transmission validated on hardware on both paths; bring-up turned up three constants outliving their cause, including a gamma default under which the amber heartbeat had never once been emitted
