- [[subghz-collar-remote-clone]] — closed the last RMT question (the C3's loop counter reports but does not stop, so the driver stops the channel from its own ISR) and wrote the migration: one contiguous 132-frame hardware loop, three timing knobs collapsed into one, radio logic shared between both firmware paths, and both paths forced onto APB after finding the status LED already owns the RMT group clock — plus the payload rebuilt at the canonical frame cut, the old array having been the same frame rotated by two runs. Nothing flashed yet.
