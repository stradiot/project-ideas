- [[home-assistant-rotary-controller]] — questionnaire Q18 to Q23 (23 of 27): enum lists as a fourth carousel level, no age on the glass anywhere since push makes age measure change-frequency rather than trust, conditional corner chrome derived from state rather than latched to events a sleeping device cannot receive, and a link drop that gates commits instead of queueing stale intent.
