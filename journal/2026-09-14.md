- [[home-assistant-rotary-controller]] — stream transport settled on `subscribe_trigger` naming groups, and the idle window showed the traffic it removes is self-inflicted rather than the instance growth the argument had rested on
