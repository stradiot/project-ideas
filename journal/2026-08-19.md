- [[home-assistant-rotary-controller]] — walked the LVGL render-loop/message design (ownership model, fact messages, doorbell-plus-cache), caught it being derived without a product spec, and built a 27-question requirements questionnaire for next session instead of continuing.
- [[home-assistant-rotary-controller]] — decided LVGL's draw-buffer and tick architecture (partial mode, two SRAM buffers, hand-wired pull tick over `esp_lvgl_port`'s push tick), scaffolded LVGL 9.5.0 into the build, caught an ungitignored secrets file, and pushed the repo for the first time.
- [[home-assistant-rotary-controller]] — settled LCD_RST (GPIO40 is not it) and panel geometry (x_gap 35) empirically, fixed an async-DMA tearing bug, decoded the encoder in hardware via PCNT with measured bounce/resolution/direction, and wrote the repo's CLAUDE.md.
