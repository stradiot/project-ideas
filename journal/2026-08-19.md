- [[home-assistant-rotary-controller]] — settled LCD_RST (GPIO40 is not it) and panel geometry (x_gap 35) empirically, fixed an async-DMA tearing bug, decoded the encoder in hardware via PCNT with measured bounce/resolution/direction, and wrote the repo's CLAUDE.md.
